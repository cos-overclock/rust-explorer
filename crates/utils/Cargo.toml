[package]
name = "rust-explorer-utils"
version = "0.1.0"
edition = "2024"
description = "Utility functions and error handling for rust-explorer"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"